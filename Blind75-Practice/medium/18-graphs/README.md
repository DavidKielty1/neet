# Graphs (medium)

**Pattern:** Graphs

**Planned content (later):** Medium graphs: BFS/DFS, number of islands, course schedule, rotting oranges, clone graph.

**Structure:** When populated, mirror `easy/` with `Python/`, `Java/`, and `tests/` as needed.

**Status:** Scaffold only—no solutions yet.
