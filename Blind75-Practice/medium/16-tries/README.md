# Tries (medium)

**Pattern:** Tries

**Planned content (later):** Trie problems (often **medium** on LeetCode): implement trie, search words, prefix tree, autocomplete-style.

**Structure:** When populated, mirror `easy/` with `Python/`, `Java/`, and `tests/` as needed.

**Status:** Scaffold only—no solutions yet.
