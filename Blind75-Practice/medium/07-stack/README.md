# Stack (medium)

**Pattern:** Stack

**Planned content (later):** Medium stack: monotonic stack, decode string, daily temperatures, largest rectangle in histogram.

**Structure:** When populated, mirror `easy/` with `Python/`, `Java/`, and `tests/` as needed.

**Status:** Scaffold only—no solutions yet.
