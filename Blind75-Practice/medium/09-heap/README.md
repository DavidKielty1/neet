# Heap / priority queue (medium)

**Pattern:** Heap / priority queue

**Planned content (later):** Medium heap: merge k sorted, top k frequent, task scheduler, meeting rooms II.

**Structure:** When populated, mirror `easy/` with `Python/`, `Java/`, and `tests/` as needed.

**Status:** Scaffold only—no solutions yet.
