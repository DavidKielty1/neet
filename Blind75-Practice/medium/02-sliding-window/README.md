# Sliding window (medium)

**Pattern:** Sliding window

**Planned content (later):** Medium sliding-window problems: variable window, min window substring, permutation in string, etc.

**Structure:** When populated, mirror `easy/` with `Python/`, `Java/`, and `tests/` as needed.

**Status:** Scaffold only—no solutions yet.
