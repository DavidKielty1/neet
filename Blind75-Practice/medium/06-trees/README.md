# Trees (medium)

**Pattern:** Trees

**Planned content (later):** Medium tree: BST ops, build from traversal, path sums, LCA on binary tree.

**Structure:** When populated, mirror `easy/` with `Python/`, `Java/`, and `tests/` as needed.

**Status:** Scaffold only—no solutions yet.
