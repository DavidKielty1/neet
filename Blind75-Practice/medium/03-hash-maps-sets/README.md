# Hash maps and sets (medium)

**Pattern:** Hash maps and sets

**Planned content (later):** Medium problems emphasizing frequency maps, groupings, and set algebra (e.g. group anagrams, longest consecutive).

**Structure:** When populated, mirror `easy/` with `Python/`, `Java/`, and `tests/` as needed.

**Status:** Scaffold only—no solutions yet.
