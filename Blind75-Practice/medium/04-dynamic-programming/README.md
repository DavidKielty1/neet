# Dynamic programming — arrays track (medium)

**Pattern:** Dynamic programming (arrays track)

**Planned content (later):** Legacy mirror of `easy/` layout; use for **1-D / array DP** mediums if you split from `10-dynamic-programming`.

**Structure:** When populated, mirror `easy/` with `Python/`, `Java/`, and `tests/` as needed.

**Status:** Scaffold only—no solutions yet.
