# Linked list (medium)

**Pattern:** Linked list

**Planned content (later):** Medium list: merge k lists, reorder lists, copy list with random pointer.

**Structure:** When populated, mirror `easy/` with `Python/`, `Java/`, and `tests/` as needed.

**Status:** Scaffold only—no solutions yet.
