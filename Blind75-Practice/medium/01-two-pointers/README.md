# Two pointers (medium)

**Pattern:** Two pointers

**Planned content (later):** NeetCode 250 / LeetCode **medium** two-pointer problems: sorted arrays, three-sum variants, interval-style scans, water/container style problems.

**Structure:** When populated, mirror `easy/` with `Python/`, `Java/`, and `tests/` as needed.

**Status:** Scaffold only—no solutions yet.
