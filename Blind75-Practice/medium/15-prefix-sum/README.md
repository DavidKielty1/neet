# Prefix sum (medium)

**Pattern:** Prefix sum

**Planned content (later):** Medium prefix sums: range queries, subarray sum equals k, matrix prefix sums.

**Structure:** When populated, mirror `easy/` with `Python/`, `Java/`, and `tests/` as needed.

**Status:** Scaffold only—no solutions yet.
