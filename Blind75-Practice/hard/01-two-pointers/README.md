# Two pointers (hard)

**Pattern:** Two pointers

**Planned content (later):** Hard two-pointer problems from the roadmap (e.g. constrained subarrays, complex multi-pass array/string problems).

**Structure:** When populated, mirror `easy/` with `Python/`, `Java/`, and `tests/` as needed.

**Status:** Scaffold only—no solutions yet.
