# Dynamic programming — arrays track (hard)

**Pattern:** Dynamic programming (arrays track)

**Planned content (later):** Array DP problems at hard difficulty. Consider merging with `10-dynamic-programming` when populating.

**Structure:** When populated, mirror `easy/` with `Python/`, `Java/`, and `tests/` as needed.

**Status:** Scaffold only—no solutions yet.
