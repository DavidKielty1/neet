# Binary search (hard)

**Pattern:** Binary search

**Planned content (later):** Hard binary search / monotonic search problems (e.g. split array, aggressive cows style).

**Structure:** When populated, mirror `easy/` with `Python/`, `Java/`, and `tests/` as needed.

**Status:** Scaffold only—no solutions yet.
