# Graphs (hard)

**Pattern:** Graphs

**Planned content (later):** Hard graph: word ladder, shortest path variants, alien dictionary (sometimes medium).

**Structure:** When populated, mirror `easy/` with `Python/`, `Java/`, and `tests/` as needed.

**Status:** Scaffold only—no solutions yet.
