# Prefix sum (hard)

**Pattern:** Prefix sum

**Planned content (later):** Hard prefix/suffix combinations with hash maps or constraints.

**Structure:** When populated, mirror `easy/` with `Python/`, `Java/`, and `tests/` as needed.

**Status:** Scaffold only—no solutions yet.
