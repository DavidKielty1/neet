# Arrays and hashing (hard)

**Pattern:** Arrays and hashing

**Planned content (later):** Hard array + hash combinations (e.g. counting subarrays with constraints).

**Structure:** When populated, mirror `easy/` with `Python/`, `Java/`, and `tests/` as needed.

**Status:** Scaffold only—no solutions yet.
